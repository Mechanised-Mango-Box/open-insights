# Extras for model_training/ only, installed on top of the runtime set:
#   pip install -r requirements.txt -r requirements-training.txt
#
# Just the plotting library now. scikit-learn, pandas, numpy and joblib moved to
# requirements.txt when the server started loading the engagement model at
# startup (inference.py) - see the note there.

matplotlib==3.11.1
